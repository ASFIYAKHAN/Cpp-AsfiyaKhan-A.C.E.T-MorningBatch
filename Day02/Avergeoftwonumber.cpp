#include <iostream>
using namespace std;

int main() {
    int num1, num2, average;
    cout << "Enter the two numbers: ";
    cin >> num1 >> num2;
    average = (num1 + num2) / 2;
    cout << "The average of the two numbers is: " << average << endl;
    return 0;
}
